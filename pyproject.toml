[project]
name = "picostatus"
version = "0.1.0"
description = "Little toy status for RPI Pico 2 and SSD1305 display"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [ "pyserial" ]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["ALL"]
ignore = ["CPY"]


